# === DATABASE (PostgreSQL) ===
# Výchozí nastavení používá PostgreSQL.
# Pro lokální vývoj mimo Docker spusť Postgres (např. `docker compose up -d postgres`)
# a nech `DATABASE_URL` ukazovat na localhost:
#   postgresql+psycopg2://gymuser:gympass@localhost:5432/gym_turnstile
# V Docker Compose (web kontejner) je host `postgres` - viz docker-compose.local.yml
DATABASE_URL=postgresql+psycopg2://gymuser:gympass@localhost:5432/gym_turnstile

# === JWT ===
# DŮLEŽITÉ: Změň na silné heslo pro produkci (min. 32 znaků)
JWT_SECRET_KEY=super_secret_key_change_me_in_production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
OWNER_ACCESS_TOKEN_EXPIRE_MINUTES=60

# === APP ===
ENVIRONMENT=development
LOG_LEVEL=info
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3000

# === OWNER & BRANDING CONFIG ===
# Owner účet se vytvoří automaticky při startu, pokud jsou tyto proměnné nastavené
OWNER_EMAIL=owner@example.com
OWNER_PASSWORD=change_me
OWNER_NAME=Platform Owner

# Upload složka pro loga (uložena uvnitř kontejneru)
BRANDING_UPLOAD_DIR=static/branding
BRANDING_LOGO_MAX_BYTES=1048576

# Výchozí branding hodnoty, použijí se při prvním startu nebo jako fallback
BRANDING_DEFAULT_BRAND_NAME=Gym Access
BRANDING_DEFAULT_CONSOLE_NAME=Control Console
BRANDING_DEFAULT_TAGLINE=Smart access management
BRANDING_DEFAULT_SUPPORT_EMAIL=support@example.com
BRANDING_DEFAULT_PRIMARY_COLOR=#0EA5E9
BRANDING_DEFAULT_FOOTER_TEXT=© 2025 GymScanner
BRANDING_DEFAULT_LOGO_URL=

# === COMGATE PAYMENT GATEWAY ===
# Získejte tyto hodnoty z vašeho Comgate účtu
COMGATE_MERCHANT_ID=
COMGATE_SECRET=
COMGATE_TEST_MODE=true

# URL pro návrat uživatele po platbě
# POZNÁMKA: Pro lokální vývoj použijte ngrok nebo podobný nástroj
# Pro produkci použijte vaši veřejnou doménu (např. https://tvoje-domena.cz)
COMGATE_RETURN_URL=https://localhost/api/payments/comgate/return

# URL pro notifikace z Comgate (POST callback)
# POZNÁMKA: Tato URL musí být veřejně dostupná!
# Pro lokální vývoj: použijte ngrok (viz výše)
# Pro produkci: použijte vaši veřejnou doménu
COMGATE_NOTIFY_URL=https://localhost/api/payments/comgate/notify
